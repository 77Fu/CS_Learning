//贪心算法：用局部最优 来代替全局最优
/*
int maxProfit(int* prices, int pricesSize) {
    int min = prices[0],earn = 0;

    for(int i = 0; i < pricesSize; i++){
        if(prices[i] < min){
            min = prices[i];
        }
        if(earn < prices[i] - min){
        earn = prices[i] - min;
        }
       
    }   
    return earn;     
}


*/
